# artifacts/

Global artifacts produced by agents that aren't tied to a single business. Per-business
artifacts live in `businesses/<slug>/artifacts/`. Runtime artifacts are git-ignored
(`artifacts/runtime/`).
